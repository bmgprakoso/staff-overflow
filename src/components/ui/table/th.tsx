import styled from "@emotion/styled";

export const Th = styled.th`
  padding: 12px 15px;
  text-align: left;
  border-bottom: 1px solid #e1e1e1;

  &:first-of-type {
    padding-left: 0;
  }

  &:last-of-type {
    padding-right: 0;
  }
`;
